<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="member">
	<insert id="insertMem" parameterType="user" >
		insert into user (email,nickname,password) values (#{email},#{password},#{nickname})
	</insert>
	
	<select id="selectEmail" parameterType="String" resultType="user">
		select email,password,nickname from user where id=#{email}
	</select>
	
	<update id="updateUser" parameterType="user">
		update member set password=#{password},nickname=#{nickname} where email=#{email}
	</update>
	
	<select id="selectAll" resultType="user">
		select id,password,nickname from user
	</select>
	
	<delete id="deleteUser" parameterType="String">
		delete from user where email=#{email}
	</delete>
	
</mapper> 