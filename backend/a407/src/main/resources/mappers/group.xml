<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="com.ssafy.a407.dao.GroupDao">
 	<select id="removeGroup" parameterType="map">delete from user where gid = #{gid}</select>
 	<select id="updateGroup" parameterType="map">update group set max = #{max}, edate = #{edate}, groupname = #{groupname}, groupDesc = #{groupDesc}, img = #{img}, category = #{category}, region = #{region} where gid = #{gid}</select>
 </mapper> 