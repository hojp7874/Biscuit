<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="com.ssafy.a407.dao.GroupDao">
 
 
 	<insert id="insertGroup" parameterType="GroupDto">
 		insert into group (email, max, sdate, edate, groupname, groupdesc, category, onoff, region, img)
 		values (#{email},#{max},now(),#{edate},#{groupname},#{groupdesc},#{category},#{onoff},#{region},#{img})
 	</insert>
 	
 	<select id="selectMemberList" parameterType="int" resultType="MemberDto">
 		select mId, email, gId
 		from member
 		where gId = #{gId}
 	</select>
 </mapper> 